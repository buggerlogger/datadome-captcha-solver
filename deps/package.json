{
  "//": "codec.js and recover.js are CommonJS. The root package.json sets \"type\": \"module\", which would otherwise make Node parse these as ESM and fail on require(). This scopes just this directory back to CommonJS.",
  "type": "commonjs"
}
